# sandbox

Ad-hoc, throwaway scripts used while exploring kg-gen's API (`t3.ts`–`t6.ts`).
Not part of any example app — run with `npx ts-node examples/sandbox/t3.ts`.
Kept for reference; safe to delete.
